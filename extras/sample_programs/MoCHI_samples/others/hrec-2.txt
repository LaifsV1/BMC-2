Methods:
f (g:Int->Int) :(Int->Int) =
  fun (x:Int) :(Int) -> if x>=0 then g x else let (f':Int->Int) = (f g) in (g x);
succ (x:Int) :(Int) = x + 1;

Main () :(Int):
let (f':Int->Int) = f succ in
f' 5
