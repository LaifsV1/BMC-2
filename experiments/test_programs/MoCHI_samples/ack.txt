Methods:
ackermann (m:Int , n:Int) :(Int) =
    if m
    then
      if n
      then ackermann (m + -1,(ackermann (m,n + -1)))
      else ackermann (m + -1,1)
    else n+1;

Main:(Unit):
let (m:Int) = 1 in
let (n:Int) = 1 in
if m >= 0 then
   if n >= 0 then
      if (ackermann (m,n) >= n) then skip
      else fail
   else skip
else skip
