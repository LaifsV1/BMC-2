(**DOESN'T WORK**)
Methods:
f (x:Int,g:Int->Unit) :(Unit) = g (x+1);
h (z:Int,y:Int) :(Unit) = if y <= z then fail else skip;

Main:(Unit):
let (n:Int) = 5 in
if n>=0 then f(n,h n) else skip
(*no partial application, can't do 'h n'*)
